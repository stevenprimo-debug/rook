<!--
Template for `~/.claude/CLAUDE.md` — the OPERATOR-PERSONAL global instructions
that live OUTSIDE the product folder per Anthropic canonical pattern.

This file is loaded into every Claude Code session globally, regardless of
which project the operator is working in. It carries the personal-identity
layer (name, schedule, family, ADHD prefs) that should never ship with the
product but should follow the operator across all their work.

The onboarding agent writes this to `~/.claude/CLAUDE.md` directly. If a file
already exists at that path, the onboarding agent reads it, finds the
`<!-- rook-onboarding:begin -->` marker, and replaces just that block. Other
content the operator has added to their global CLAUDE.md is preserved.
-->

<!-- rook-onboarding:begin -->

# {{name}} — Personal Context (Global)

> Generated by ROOK onboarding {{generated_at}}. This file is OUTSIDE the ROOK product folder by design — it follows you across every Claude Code session, every project. Don't move this into the product folder; that's what `PrimoLabs_PoweredByClaude/CLAUDE.md` is for (architectural context).

## Who I am

- **Name:** {{name}}
- **Role:** {{role}}
- **Mission right now:** {{mission_sentence}}

## My schedule

- Workday: {{workday_start}}–{{hard_stop}} ({{timezone}})
- Off-days: {{off_days}}
- Weekly anchor: {{weekly_anchor}}

## How I work

- **Hard-stop reminder:** {{hard_stop_reminder}} — when this is true, agents should flag at {{hard_stop_minus_30}} that the workday is wrapping up
- **Drop polish under pressure:** {{drop_polish_when_fast}} — when I'm live with a client, on a deadline, or moving fast, ship 80% over wait at 100%
- **Biggest constraint right now:** {{biggest_constraint}}

## What never ships with ROOK

- My name (`{{name}}`) — only used in greetings, sign-offs, and this file. Never in agent prompts that produce customer-facing output.
- My company name (`{{company_brand}}`) — only used in MY proposals/contracts, never in ROOK product files.
- My specific financial targets, exit goals, or family details — operator-personal only.

This file is the right place for operator-specific state. The product folder is the right place for product code + customer-tunable configuration.

<!-- rook-onboarding:end -->
