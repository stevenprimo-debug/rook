<!--
Template for the personalized section appended to `CLAUDE.md` at the product root.
The onboarding agent appends this block BELOW the existing architectural section
(don't replace; append). Section is marked with HTML comment delimiters so future
re-runs of onboarding can find and replace just this block without touching the
rest of the file.

Delimiters:
  <!-- onboarding:begin --> ... <!-- onboarding:end -->
-->

<!-- onboarding:begin -->

## This installation

Generated by the onboarding interview on {{generated_at}}. To regenerate, re-run `setup/onboarding/`.

**Operator:** {{name}} — {{role}}
**Current mission:** {{mission_sentence}}
**Company / brand:** {{company_brand}}
**Industry:** {{industry}}
**Target:** ${{revenue_target_12mo}} revenue by {{target_date}}
**Voice mode:** `{{voice_mode_slug}}` (defined in each agent's `personality/voice_modes/{{voice_mode_slug}}.md`)

### Schedule

- Workday: {{workday_start}}–{{hard_stop}} ({{timezone}})
- Off-days: {{off_days}}
- Weekly anchor: {{weekly_anchor}}
- Hard-stop reminder: {{hard_stop_reminder}}

### Working mode

- Drop polish when moving fast: {{drop_polish_when_fast}}
- Biggest current constraint: {{biggest_constraint}}

### Tooling

- CRM / prospecting: {{crm_tool}}

### Standing context for every session

Every agent loads `agents/<agent>/context/00-operator-profile.md` on session start. That file is the per-agent view of this profile. Together with the routing-rules manifest, that's how every session begins already aware of who you are and what you're working on.

<!-- onboarding:end -->
