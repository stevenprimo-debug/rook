---
name: methodology-shelf
purpose: cross-cutting methodology promotion target
populated-by: librarian shelf-promote mode (manual operator confirm required)
---

# Cross-cutting methodology shelf

This folder is RESERVED for methodologies validated across 2+ agents.

Single-agent methodologies (one agent owns the pattern) belong at:
`agents/<agent>/context/methodology/<pattern>.md`

## Promotion criteria (librarian shelf-promote mode)

A pattern earns a slot here when:
1. 2+ agents have independently developed memory entries about the same concept
2. The librarian's graphify sweep detects `semantically_similar_to` edges across agents
3. The operator confirms the promotion (reversibility=N — patterns here become canonical)

## Current shelf state

Empty at 2026-05-22 (post-relocation). First promotion candidates surface in the next librarian weekly sweep.

## See also

- `agents/librarian/SKILL.md` shelf-promote mode
- `agents/<agent>/context/methodology/` for single-agent methodology
